# Credets — AI Context File

> **Purpose:** This file provides full project context so that any AI assistant can understand the Credets monorepo web application — its architecture, conventions, current implementation state, and the best practices it follows. Feed this file to an AI chat before asking it to work on this project.

---

## 1. Project Overview

**Credets** is a personal credentials manager — a single-user web application for securely storing, organizing, and managing credentials, API keys, SSH keys, media assets, game loadouts, and other sensitive data. The name is a shorthand for "credentials."

**Core idea:** A monorepo web app where the backend exposes a REST API (Bun runtime) and the frontend consumes it (TanStack Router + React 19). The project emphasizes web security best practices since it holds sensitive information.

**Current state:** The app supports listing credentials with cursor-based pagination and infinite scroll, viewing credential details with image lightbox, and creating new credentials with a flexible data-block form. Update and delete are scaffolded but not yet fully implemented. Authentication (login/signup) is designed but not yet wired into route guards.

---

## 2. Monorepo Architecture

```
credets/                          ← root (bun workspaces)
├── apps/
│   ├── backend/                  ← Bun HTTP server, raw PostgreSQL
│   │   ├── db/                   ← init.sql, connection.ts, seed.ts
│   │   ├── http/                 ← route handlers (credentials/, csrf/, types/)
│   │   ├── validation/           ← server-side Zod validation endpoints
│   │   ├── utils/                ← encrypt, decrypt, processImage, parseLocalDate, response
│   │   ├── types/                ← response type definitions
│   │   └── index.ts              ← Bun.serve entry point with route definitions
│   └── frontend/                 ← TanStack Router + React 19 SPA
│       ├── src/
│       │   ├── routes/           ← file-based routing (TanStack Router)
│       │   │   ├── __root.tsx    ← root layout (QueryClientProvider, DevTools, Toast)
│       │   │   ├── index.tsx     ← home page
│       │   │   └── credentials/  ← credential CRUD routes
│       │   │       ├── index.tsx                ← listings with infinite scroll
│       │   │       ├── $credentialId/
│       │   │       │   ├── index.tsx            ← detail view
│       │   │       │   ├── update/index.tsx     ← update (scaffolded)
│       │   │       │   ├── -components/         ← credential-data, image-lightbox
│       │   │       │   └── -actions/            ← getCredential
│       │   │       ├── create/
│       │   │       │   ├── index.tsx            ← create form
│       │   │       │   ├── -components/         ← DataBlock
│       │   │       │   ├── -actions/            ← createCredentialAction, validation, CSRF, types
│       │   │       │   └── -utils/              ← createCredentialFormdata, isDatablockEmpty
│       │   │       └── -components/             ← credential-card (shared by listing)
│       │   │       └── -actions/                ← getCredentialsListings
│       │   ├── components/ui/    ← shadcn/ui components (button, card, field, form, etc.)
│       │   ├── lib/utils.ts      ← cn() utility (clsx + tailwind-merge)
│       │   ├── router.tsx        ← router factory (not used in main.tsx yet)
│       │   ├── main.tsx          ← app entry point
│       │   └── routeTree.gen.ts  ← auto-generated by TanStack Router plugin
│       ├── vite.config.ts
│       ├── components.json       ← shadcn/ui config
│       ├── biome.json            ← frontend-specific Biome config
│       └── tsconfig.json
├── packages/
│   ├── shared-schema/            ← @credets/shared-schema — Zod validation schemas
│   │   └── src/credentials/create.ts
│   ├── shared-types/             ← @credets/shared-types — TypeScript types (zod-inferred)
│   │   └── src/credentials/
│   │       ├── create.ts         ← CredentialCreateType
│   │       └── listings.ts       ← CredentialListItem, CredentialDetail, CredentialListingsResponse
│   └── shared-utils/             ← @credets/shared-utils — shared utility functions
├── docs/                         ← project documentation
│   ├── app.md                    ← app story, stacks, requirements, features
│   ├── credentials-resource.md   ← credential form fields spec
│   ├── authentication.md         ← auth system design (single-user, special password)
│   ├── databases.md              ← DB schema, access, seed guide
│   ├── form.md                   ← form handling patterns (frontend + backend)
│   ├── offset-pagination.md      ← offset pagination reference (legacy)
│   ├── dblab.md                  ← database TUI tool guide
│   └── #todo.md                  ← pending tasks
├── docker-compose.yml            ← PostgreSQL 18 in Docker
├── makefile                      ← db-up / db-down shortcuts
├── biome.json                    ← root Biome config
├── tsconfig.json                 ← root TypeScript config with monorepo path aliases
├── package.json                  ← root workspace config with scripts
├── knowledge.md                  ← Codebuff context file
└── README.md                     ← setup guide
```

---

## 3. Tech Stack

### Backend

- **Runtime:** Bun (with `--watch` for dev)
- **Database:** PostgreSQL 18 (Docker), accessed via Bun's built-in `SQL` tagged template literals (no ORM)
- **Validation:** Zod v4 (via catalog)
- **Encryption:** `iron-webcrypto` for seal/unseal of special password
- **Image Processing:** Bun's native `Bun.Image` — resize + convert to WebP
- **CSRF:** Bun's built-in `Bun.CSRF.generate()` / `Bun.CSRF.verify()`
- **Password Hashing:** Bun's built-in `Bun.password.hash()` / `Bun.password.verify()`
- **Date Handling:** `date-fns`
- **Port:** 8000

### Frontend

- **Framework:** React 19
- **Routing:** TanStack Router (file-based, auto-code-splitting via Vite plugin)
- **Data Fetching:** TanStack Query v5 (with `gcTime: 0`, `staleTime: 0` — no client cache)
- **Forms:** TanStack Form v1 (with Zod adapter for validation)
- **UI Library:** shadcn/ui (base-luma style) + Base UI
- **Styling:** Tailwind CSS v4 + `tw-animate-css`
- **Animation:** Framer Motion
- **Icons:** Lucide React
- **Toast:** goey-toast (GooeyToaster)
- **DevTools:** TanStack DevTools (Router + Form panels)
- **Build:** Vite 8
- **Testing:** Vitest + Testing Library
- **Linting:** Biome (tabs, double quotes, 80/100 line width)
- **Port:** 3000

### Shared Packages

- `@credets/shared-schema` — Zod schemas (source of truth for validation)
- `@credets/shared-types` — TypeScript types inferred from Zod schemas
- `@credets/shared-utils` — shared utility functions

### DevOps & Tooling

- **Package Manager:** Bun (workspaces)
- **Containerization:** Docker Compose (PostgreSQL only)
- **Linting/Formatting:** Biome 2.4.5 (root + frontend override)
- **TypeScript:** v6 (catalog)
- **Database Inspection:** dblab (TUI)

---

## 4. Backend API Endpoints

All endpoints are defined in `apps/backend/index.ts` using Bun's `routes` object.

| Method | Path | Handler | Status |
|--------|------|---------|--------|

| GET | `/` | Static HTML (index.html) | Working |
| GET | `/get-csrf` | `generateCSRF()` | Working |
| GET | `/credentials` | `credentialListings(req)` — cursor-based pagination | Working |
| GET | `/credentials/:credentialId` | `credentialPage(req)` — full detail with images | Working |
| POST | `/credentials/create` | `credentialCreate(req)` — FormData with CSRF, Zod, image processing | Working |
| POST | `/credentials/create/validation` | `createCredentialValidation(req)` — server-side form validation | Working |
| GET | `/credentials/update` | `credentialUpdate()` — scaffolded, special password check logic | Scaffolded |
| GET | `/credentials/delete` | `credentailDelete(req, server)` — scaffolded | Scaffolded |
| GET | `/types/listings` | `typesListings()` — returns all credential types | Working |

**Response format (success):**

```json
{
  "success": true,
  "data": { ... },
  "message": "...",
  "timestamp": "ISO-8601",
  "path": "/credentials",
  "status": 200
}
```

**Response format (error):**

```json
{
  "success": false,
  "error": "error category",
  "message": "human-readable message",
  "timestamp": "ISO-8601",
  "details": { "originError": "..." },
  "data": {},
  "path": "/credentials",
  "status": 500
}
```

**Response Factory:** All responses go through `ResponseFactory.success()` or `ResponseFactory.error()` in `apps/backend/utils/response.ts`.

---

## 5. Database Schema

PostgreSQL 18, initialized via `apps/backend/db/init.sql`.

### Tables

**`users`** — Single user only

| Column | Type | Notes |
|--------|------|-------|

| id | UUID | PK, auto-generated |
| name | VARCHAR(50) | |
| username | VARCHAR(50) | UNIQUE |
| email | VARCHAR(200) | UNIQUE |
| password | text | Bun.password.hash() |
| special_password | text | iron-webcrypto sealed |
| created_at | TIMESTAMPTZ | DEFAULT NOW() |
| updated_at | TIMESTAMPTZ | DEFAULT NOW(), auto-updated via trigger |

**`session`**

| Column | Type | Notes |
|--------|------|-------|

| id | UUID | PK |
| user_id | UUID | FK → users.id, CASCADE |
| token | VARCHAR(255) | UNIQUE |
| expires_at | TIMESTAMPTZ | NOT NULL |
| created_at | TIMESTAMPTZ | DEFAULT NOW() |

**`types`** — Credential type definitions

| Column | Type | Notes |
|--------|------|-------|

| id | UUID | PK |
| label | VARCHAR(100) | UNIQUE, e.g. "API" |
| value | VARCHAR(100) | UNIQUE, e.g. "api" |
| description | TEXT | |
| created_at / updated_at | TIMESTAMPTZ | |

**`credentials`** — Main resource table

| Column | Type | Notes |
|--------|------|-------|

| id | UUID | PK |
| title | TEXT | NOT NULL |
| short_description | TEXT | NULL |
| long_description | TEXT | NULL |
| thumbnail_image_data | BYTEA | Binary, stored as WebP |
| thumbnail_format | VARCHAR(10) | Always "webp" |
| thumbnail_width / thumbnail_height | INT | |
| data | JSONB | NOT NULL — flexible structure |
| notes | TEXT | NULL |
| tags | JSONB | NULL — array of strings |
| created_at / updated_at | TIMESTAMPTZ | |
| user_id | UUID | FK → users.id, SET NULL |
| types_id | UUID | FK → types.id, SET NULL |

**`credential_images`** — Multiple images per credential

| Column | Type | Notes |
|--------|------|-------|

| id | UUID | PK |
| image_data | BYTEA | Binary, WebP |
| format | VARCHAR(10) | "webp" |
| width / height | INT | |
| byte_size | INT | |
| sort_order | INT | DEFAULT 0 |
| created_at / updated_at | TIMESTAMPTZ | |
| credential_id | UUID | FK → credentials.id, CASCADE |

### Indexes

- GIN indexes on `credentials.data` and `credentials.tags` for JSONB queries
- B-tree indexes on title, descriptions, notes, created_at, foreign keys
- Unique indexes on users.username, users.email

### Seed Data

`bun run seed` creates: 1 user, 1 session, 6 types, 500 credentials across all types with rich mock data. No images/thumbnails in seed.

---

## 6. Frontend Routes

File-based routing via TanStack Router plugin (`@tanstack/router-plugin`).

| Path | File | Component | Status |
|------|------|-----------|--------|

| `/` | `routes/index.tsx` | Home | Basic |
| `/credentials` | `routes/credentials/index.tsx` | Listings with infinite scroll | Working |
| `/credentials/$credentialId` | `routes/credentials/$credentialId/index.tsx` | Detail view with lightbox | Working |
| `/credentials/create` | `routes/credentials/create/index.tsx` | Create form | Working |
| `/credentials/$credentialId/update` | `routes/credentials/$credentialId/update/index.tsx` | Update form | Scaffolded |

**Route features:**

- `pendingComponent` — skeleton loaders during navigation
- `errorComponent` — error UI with back navigation
- `loader` — data pre-fetching before render (TanStack Router loader)
- `defaultPreload: 'intent'` — preload on hover/focus
- `scrollRestoration: true` — restore scroll position on back/forward

---

## 7. Credential Data Block System

Credentials have a flexible `data` field stored as JSONB. The create form supports three data block types:

1. **`single_label`** — A single text value (e.g., a password or API key)
2. **`key_value`** — A key-value pair (e.g., "username" → "john")
3. **`information`** — A long text block (e.g., notes or instructions)

These are defined in:

- **Schema:** `packages/shared-schema/src/credentials/create.ts` — `z.discriminatedUnion("type", [...])`
- **Types:** `packages/shared-types/src/credentials/listings.ts` — `DataBlockEntry`
- **Frontend component:** `DataBlock` in `routes/credentials/create/-components/Datablock.tsx`
- **Renderer:** `CredentialDataRenderer` in `routes/credentials/$credentialId/-components/credential-data.tsx`

The detail view also handles "flat" data objects (from seed data) using `normaliseData()` which auto-detects the shape.

---

## 8. Form Handling Pattern

This project follows a specific form pattern that must be maintained:

### Frontend (TanStack Form)

1. **Always use `FormData`** for form submissions — never JSON
2. Use `useForm()` from `@tanstack/react-form`
3. Validation uses Zod schema via `validators.onSubmit`
4. Server-side validation via `validators.onSubmitAsync` — calls a dedicated validation endpoint
5. `onSubmitAsync` **must always return something** (even if unused) for TanStack Form to work
6. For server validation errors: return `{ fields: data.errors }` so field-level errors display
7. CSRF token is fetched via `useQuery` and included as `_csrf` hidden field
8. Each field uses `form.Field` with `children` render prop pattern

### Backend

1. Dedicated validation endpoint at `/credentials/create/validation`
2. Both create and validation endpoints: read `FormData`, verify CSRF, parse with Zod
3. `formatZodError()` transforms Zod issues into `{ fieldName: [{ message: "..." }] }` format
4. Image processing: `processImage()` resizes and converts to WebP before DB storage
5. Thumbnail: max 800px width, quality 50
6. Gallery images: max 1400px width, quality 75

---

## 9. Authentication System

### Design (not fully implemented in routes yet)

- **Single-user system** — only one user exists
- **Login fields:** username, email, password, special-password
- **Special password:** Two parts — static (encrypted in DB) + dynamic (today's date)
  - Static part: sealed with `iron-webcrypto` using `ENC_KEY` env var
  - Dynamic part: today's date in `dd/MM/yyyy` format (e.g., "3/5/2026")
  - Full format: `"static part. dd/MM/yyyy"`
  - Verification: decrypt static part from DB, append server date, compare with input
- **Session:** UUID token stored in DB, attached via cookies (no Max-Age/Expires)
- **CSRF:** Bun's built-in CSRF generation/verification with 30-minute expiry

### Environment Variables

- `ENC_KEY` — 64-char hex from `openssl rand -hex 32`
- `CSRF_SECRET_KEY` — for CSRF token generation
- `DB_USER`, `DB_PASSWORD`, `DB_NAME` — PostgreSQL credentials
- `FRONTEND_APP` — frontend URL for CORS headers
- `VITE_BACKEND_APP` — backend URL for frontend fetch calls

---

## 10. Pagination

### Current: Cursor-based (infinite scroll)

- Used in `/credentials` listing endpoint and frontend
- Cursor = base64-encoded JSON `{ createdAt, id }`
- Composite cursor handles duplicate timestamps: `(created_at, id)`
- Frontend: `IntersectionObserver` on sentinel element, 200px `rootMargin` for pre-fetch
- `hasMore` flag from server, `nextCursor` for next page
- Limit: 1–24 per page, default 12

### Legacy: Offset-based (kept in comments)

- Previously used `page` / `limit` query params
- Included HATEOAS links (first, previous, next, last)
- Documented in `docs/offset-pagination.md`

---

## 11. Conventions & Gotchas

### Conventions

- **Path aliases:** Frontend uses `#/*` (e.g., `#/components/ui/button`), root uses `@credets/*` and `@db/*`, `@backend/*`
- **Formatting:** Biome — tabs, 80 line width (root) / 100 (frontend), double quotes, organize imports on save
- **shadcn/ui style:** "base-luma" with neutral base color, CSS variables enabled
- **Icon library:** Lucide React
- **Image format:** All uploaded images converted to WebP before storage
- **CORS:** Backend sets `Access-Control-Allow-Origin` from `FRONTEND_APP` env var on responses
- **File naming:** Routes follow TanStack Router convention; private components use `-components`/`-actions`/`-utils` prefix folders

### Gotchas

- No `.env` template exists — must create `.env` manually with all required vars
- `ENC_KEY` must be copied to `apps/backend/` directory
- Backend uses `bun --watch`; frontend uses Vite HMR on port 3000
- `onSubmitAsync` in TanStack Form **must always return something** even if unused
- Frontend type-check is only at build time (no separate `type-check` script); backend has `tsc --noEmit`
- `routeTree.gen.ts` is auto-generated — never edit manually, excluded from Biome
- `styles.css` is also excluded from Biome
- Tags are stored as JSONB array but submitted as comma-separated string from the form
- The `CredentialType` in shared-types is deprecated — use `CredentialListItem` instead
- `processImage()` has a TODO for later optimization with profile presets
- `DataBlock` component still uses `any` types — needs proper `FormApi` typing

---

## 12. Credential Types

Six credential types are seeded into the database:

| value | label | Description |
|-------|-------|-------------|

| `credentials` | Credentials | Standard login credentials for websites, apps, and services |
| `key` | Key | SSH keys, API keys, license keys, and encryption keys |
| `api` | API | API tokens, service accounts, and platform credentials |
| `media` | Media | Media assets, collections, and content references |
| `game_loadout` | Game Loadout | Gaming configurations, loadouts, and character builds |
| `misc` | Misc | Miscellaneous notes, configurations, and quick references |

Each type has a corresponding color scheme in the frontend (badge colors, data renderer accents).

---

## 13. UI Component Library

shadcn/ui components installed in `apps/frontend/src/components/ui/`:

- `badge`, `button`, `card`, `field` (Field, FieldLabel, FieldContent, FieldDescription, FieldError, FieldGroup), `form`, `input`, `item` (Item, ItemContent, ItemDescription, ItemMedia, ItemTitle), `label`, `select`, `separator`, `skeleton`, `spinner`, `table`, `textarea`
- Custom: `goey-toaster.tsx` (gooey toast notifications)

---

## 14. TanStack Router Best Practices & Rules

These are the practices this project follows and that AI assistants must respect when working on this codebase:

### Route Definition

- Use **file-based routing** — create route files in `src/routes/`, not programmatic routes
- Route files export `Route` created with `createFileRoute("/path")()`
- Use `Route` export for route configuration (loader, component, pendingComponent, errorComponent)
- Never edit `routeTree.gen.ts` — it is auto-generated by the Vite plugin
- Use TanStack Router's type-safe `Link` component instead of `<a>` tags
- Use `useNavigate()` for programmatic navigation
- Use `useLoaderData({ from: "/exact/path/" })` for type-safe loader data access

### Data Loading

- Use **route loaders** for data fetching — they run before the route renders
- Return data from `loader: async () => fetchData()` — this ensures data is available before render
- Use `pendingComponent` to show skeleton/placeholder UI during loading
- Use `errorComponent` to handle loader failures gracefully
- Configure `defaultPreload: 'intent'` for preloading on hover/focus
- Configure `scrollRestoration: true` for proper back/forward navigation

### Router Configuration

- Register the router type via `declare module '@tanstack/react-router' { interface Register { router: typeof router } }`
- Use `autoCodeSplitting: true` in the Vite plugin for optimal bundle splitting
- Use the `@tanstack/router-plugin/vite` plugin, NOT the legacy `@tanstack/router-vite-plugin`

### Search Params & Navigation

- Use `useSearch()` and `useNavigate()` for type-safe search param access
- Use `Link` component's `search` prop for passing search params
- Use `redirect()` in loaders for auth guards (future implementation)

### Current Router Config

```ts
const router = createRouter({
  routeTree,
  defaultPreload: 'intent',
  scrollRestoration: true,
})
```

---

## 15. TanStack Query Best Practices & Rules

### Query Configuration

- This project intentionally disables client-side caching: `gcTime: 0`, `staleTime: 0`
- This means every mount triggers a fresh fetch — appropriate for credentials data that must always be current
- If caching is needed later, use `staleTime` and `gcTime` per-query, not globally

### Query Usage Patterns

- Use `useQuery` for data fetching in components (e.g., CSRF token, types listings)
- Use route `loader` + `fetch` for critical page data (e.g., credential listings, credential detail)
- Route loaders bypass TanStack Query — they directly fetch and return data
- For mutations, use direct `fetch` calls in form submit handlers (not `useMutation`)
- Toast notifications for async feedback: `gooeyToast.promise()` for create operations, `gooeyToast.error()` for failures

### Infinite Scroll Pattern

- Initial data from route loader
- Additional pages fetched via the same fetch function with `cursor` param
- State: `credentials[]`, `nextCursor`, `hasMore`, `isLoadingMore`, `loadError`
- `IntersectionObserver` on a sentinel div with `rootMargin: "200px"` for pre-fetch
- Use `useRef` to prevent duplicate fetches (`loadingRef`)

---

## 16. TanStack Form Best Practices & Rules

### Form Setup

- Use `useForm()` with `defaultValues` — must match the form data structure
- Use `validators.onSubmit` for client-side Zod schema validation
- Use `validators.onSubmitAsync` for server-side validation via dedicated endpoint
- `onSubmitAsync` must **always return something** — even `return {}` — or the form breaks
- For server field errors, return `{ fields: { fieldName: [{ message: "..." }] } }`

### Field Pattern

```tsx
<form.Field
  name="fieldName"
  children={(field) => {
    const isInvalid = !field.state.meta.isValid;
    return (
      <Field data-invalid={isInvalid}>
        <FieldLabel htmlFor="fieldName">Label</FieldLabel>
        <Input
          id="fieldName"
          value={field.state.value}
          onBlur={field.handleBlur}
          onChange={(e) => field.handleChange(e.target.value)}
          aria-invalid={isInvalid}
        />
        {isInvalid && <FieldError errors={field.state.meta.errors} />}
      </Field>
    );
  }}
/>
```

### Array Fields

- Use `mode="array"` on `form.Field` for array data (e.g., `data` blocks)
- Use `arrayField.pushValue()` to add items
- Use `arrayField.removeValue(idx)` to remove items
- Access values via `arrayField.state.value`

### CSRF Integration

- Fetch CSRF token via `useQuery` before rendering form
- Include as hidden `_csrf` field in the form
- Backend verifies CSRF before processing any form submission

---

## 17. React Best Practices & Rules

### Component Design

- Use **function components** only — no class components
- Use **named exports** for components (not default exports)
- Keep components focused — one component per file
- Extract reusable logic into custom hooks
- Use `useCallback` for event handlers passed as props or used in dependency arrays
- Use `useMemo` for expensive computations
- Use `useRef` for mutable values that don't trigger re-renders (e.g., observer refs, loading flags)

### State Management

- Use route loaders for server state (primary pattern in this project)
- Use TanStack Query for client-side async state (secondary pattern)
- Use `useState` for local UI state only (e.g., lightbox open/close)
- Never duplicate server state in React state — derive from loader data

### Rendering

- Use `key` prop with stable, unique IDs (not array index)
- Use conditional rendering with `&&` for simple cases, ternary for if/else
- Use `line-clamp-*` Tailwind classes for text truncation
- Use `truncate` class for single-line truncation

### Accessibility

- Always set `aria-invalid` on form inputs when validation fails
- Use `htmlFor` on labels matching input `id`
- Use `role="dialog"` and `aria-modal="true"` on modals/lightboxes
- Provide `aria-label` for icon-only buttons

### Performance

- Use `import.meta.env.VITE_BACKEND_APP` for backend URL (Vite env variable)
- Use React 19's automatic batching (default behavior)
- Prefer CSS transitions over JavaScript animations for simple effects
- Use `transition-all duration-200` for hover states (Tailwind pattern in this project)

---

## 18. TypeScript Configuration

### Root tsconfig.json

- `strict: true` enabled
- `noUncheckedIndexedAccess: true`
- `noImplicitOverride: true`
- `verbatimModuleSyntax: true`
- Monorepo path aliases: `@credets/shared-types/*`, `@credets/shared-utils/*`, `@credets/shared-schema/*`, `@db/*`, `@backend/*`

### Frontend tsconfig.json

- `noUnusedLocals: true`, `noUnusedParameters: true`
- `noUncheckedSideEffectImports: true`
- Path alias: `#/*` → `./src/*`
- Additional aliases for shared packages

---

## 19. Biome Configuration

### Root (biome.json)

- Tab indentation, 80 line width, double quotes
- `organizeImports: "on"`
- Some rules disabled: `noStaticOnlyClass`, `noChildrenProp`, `noNonNullAssertion`

### Frontend (apps/frontend/biome.json)

- Overrides root: 100 line width
- Includes: `**/src/**/*`, `**/index.html`, `**/vite.config.ts`
- Excludes: `**/src/routeTree.gen.ts`, `**/src/styles.css`
- VCS disabled for frontend-specific config

---

## 20. Requirements & Feature Checklist

### Requirements (from docs/app.md)

- [x] Create a web application for securely storing credentials
- [x] Create new credential
- [ ] Update existing credentials (scaffolded, logic not implemented)
- [ ] Delete unnecessary credentials (scaffolded, logic not implemented)
- [ ] Full auth and authorization for each route (designed, not wired)
- [x] Media uploading support (thumbnail + gallery images)
- [ ] Bulk export credentials with email
- [x] Flexible credential input (data block system)

### Features (from docs/app.md)

1. [x] Credentials listings (with cursor-based infinite scroll)
2. [x] Create new credential (full form with validation)
3. [ ] Update existing credential
4. [ ] Delete old credential
5. [x] Image support for credentials (thumbnail + gallery with lightbox)
6. [ ] Searching, sorting, filtering
7. [ ] Bulk export support with email

### Pending Tasks (from docs/#todo.md)

- [ ] Auto-focus on data block inputs when added
- [ ] Add information about comma-separated tags
- [ ] Add manual types for `item`, `form` in DataBlock component
- [ ] Add proper props for submit button (loading state, etc.)
- [ ] Show placeholder when no data blocks exist
- [x] Show server validation errors in frontend form
- [ ] Hide timestamp from gooey toast

---

## 21. Scripts & Commands

```bash
# Development
bun run dev                  # Run both backend + frontend concurrently
bun run dev:backend          # Backend only (port 8000, --watch)
bun run dev:frontend         # Frontend only (port 3000, Vite)

# Database
make db-up                   # Start PostgreSQL in Docker
make db-down                 # Stop PostgreSQL
bun run seed                 # Seed database with 500 credentials

# Build
bun run build                # Build both apps
bun run build:backend        # Build backend only
bun run build:frontend       # Build frontend only

# Quality
bun run lint                 # Biome lint (frontend)
bun run check                # Biome check (frontend)
bun run type-check           # TypeScript check (backend only)

# Testing
bun test                     # Run Vitest (frontend)
```

---

## 22. Key Design Decisions

1. **No ORM** — Raw SQL via Bun's `SQL` tagged template literals for maximum control and learning
2. **No client cache** — TanStack Query with `gcTime: 0` / `staleTime: 0` ensures fresh data always
3. **WebP everywhere** — All uploaded images converted to WebP for smaller size
4. **FormData over JSON** — Forms submit as `multipart/form-data` to support file uploads natively
5. **Custom CSRF** — Not using a library; Bun's built-in CSRF is used with manual lifecycle management
6. **Cursor pagination** — Switched from offset-based to cursor-based for better infinite scroll performance
7. **Flexible data** — JSONB `data` column + discriminated union form blocks allow any credential structure
8. **Single user** — No multi-tenancy; simplifies auth to a single special-password system
9. **Monorepo shared packages** — Zod schemas and TypeScript types are shared between frontend and backend
10. **Bun native APIs** — Using Bun.Image, Bun.CSRF, Bun.password, Bun.SQL instead of third-party libraries

---

## 23. Host & Deployment

- **Backend:** Render (planned)
- **Frontend:** Not decided yet
- **Database:** Docker Compose (local development), planned managed PostgreSQL for production
