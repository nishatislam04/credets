0. create form types input is not showing err from server. fix it
1. create a shared-utils-taglist function. with 2 variant
2. add a action toolbar above credentials listings. so that we can add
    1. view mode
    2. enable local cache
    3. etc
3. listings page, short description will always be simple plaintext. not rte content.
   so update the renderer based on that condition
4. create a shared-utils-typeColor function ^^
5. we will manually re-design the layout of single view page of sidebar items for small view
